String db = "Zhiguli;  1987;  1.3 ; false;"; // salitaut na oshibku $(dolar),  .(tocka),  ^(krishka)  (ih nujno ekranirovat  Z.B.: db.split("\\$");)
		String[] data = db.split(";");
		
		for(int i =0 ; i<data.length;i++)System.out.println(data[i]);
		
		int newYear = Integer.parseInt(data[1].trim())+5;
		System.out.println(newYear);
	}


HW:
1) zdelat blyat!!! =  class Book()
	String author;
	Strring title;
	int yaer;
	double prise
	boolean pictures;

	
	http://mySite/add ? author= "hsdjfjdf"

 //po etoy stroke vostanovit knigu (nujno rasparsit)// split, join, substring

2) public boolean corectBrockets(String) // (),{},[] pravilnaya li stroka odinakovoe kolichestvo skobok true else fals

3)

class Cat() {
 public	boolean fool(sitiy kot);
}
sosdaem massiv is 1000 koshek Cat[] cats //esli peremennaya lokalnayay nakormit vsek koshek ,esli peremennaya staticheskay nakormit vseh koshek
